<style>
    body,
    .wrapper {
        background: #0b1220 !important;
        color: #dbe7f6;
    }

    .main-header.navbar,
    .navbar-white.navbar-light {
        background: #0f172a !important;
        border-bottom: 1px solid #1f2b46 !important;
        color: #9aabc2 !important;
        box-shadow: none !important;
    }

    .main-header .nav-link {
        color: #9aabc2 !important;
    }

    .main-header .nav-link:hover {
        color: #f4f7fb !important;
        background: rgba(56, 189, 248, .08);
    }

    .main-sidebar {
        background: #0b1220 !important;
        border-right: 1px solid #1f2b46;
    }

    .brand-link {
        background: #0b1220 !important;
        border-bottom: 1px solid #1f2b46 !important;
        color: #f4f7fb !important;
    }

    .brand-link .brand-text {
        color: #f4f7fb !important;
    }

    .nav-sidebar > .nav-item > .nav-link {
        background: transparent !important;
        color: #8ea0b8 !important;
    }

    .nav-sidebar > .nav-item > .nav-link:hover,
    .nav-sidebar > .nav-item > .nav-link.active {
        background: #102a44 !important;
        color: #f4f7fb !important;
        box-shadow: inset 0 0 0 1px #1d8cf8;
    }

    .nav-sidebar .nav-icon {
        color: #8bb7e6;
    }

    .nav-sidebar > .nav-item > .nav-link:hover .nav-icon,
    .nav-sidebar > .nav-item > .nav-link.active .nav-icon {
        color: #38bdf8;
    }

    .main-footer.footer-dark {
        background: #0b1220 !important;
        border-top: 1px solid #1f2b46;
        color: #8ea0b8 !important;
    }

    .content-wrapper {
        background: #0f172a !important;
    }

    .content-header h1 {
        color: #e5eefb !important;
    }

    .card {
        background: #0f172a !important;
        border: 1px solid #1f2b46 !important;
        box-shadow: 0 18px 42px rgba(0, 0, 0, .18) !important;
    }

    .card-header {
        background: #0b1220 !important;
        border-bottom: 1px solid #1f2b46 !important;
        color: #e5eefb !important;
    }

    .card-title {
        color: #f4f7fb !important;
    }

    .card-tools .btn-tool {
        color: #8ea0b8 !important;
    }

    .card-tools .btn-tool:hover {
        color: #38bdf8 !important;
    }

    .card-body {
        background: #0f172a !important;
    }

    .identity-shell {
        color: #dbe7f6;
    }

    .identity-form {
        max-width: 860px;
        margin: 0 auto;
    }

    .identity-toolbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 18px;
        padding: 4px 0 20px;
    }

    .identity-kicker {
        display: block;
        color: #38bdf8;
        font-size: 12px;
        font-weight: 800;
        letter-spacing: .08em;
        text-transform: uppercase;
    }

    .identity-title,
    .identity-toolbar h2 {
        margin: 0;
        color: #f4f7fb;
        font-size: 26px;
        font-weight: 700;
    }

    .identity-copy,
    .identity-toolbar p {
        margin: 6px 0 0;
        color: #8ea0b8;
    }

    .identity-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: 14px;
        margin-bottom: 18px;
    }

    .identity-stat,
    .identity-table-card,
    .identity-panel,
    .identity-empty {
        border: 1px solid #24314f;
        border-radius: 8px;
        background: #111a2e;
    }

    .identity-stat {
        padding: 16px;
    }

    .identity-stat span {
        display: block;
        color: #8ea0b8;
        font-size: 13px;
    }

    .identity-stat strong {
        display: block;
        margin-top: 6px;
        color: #f4f7fb;
        font-size: 28px;
        font-weight: 800;
    }

    .identity-search {
        display: flex;
        gap: 10px;
        margin-bottom: 18px;
    }

    .identity-input,
    .identity-shell .form-control {
        min-height: 42px;
        border: 1px solid #2a3a5d;
        border-radius: 8px;
        color: #eef5ff;
        background: #0f172a;
        box-shadow: none;
        outline: none;
    }

    .identity-input::placeholder,
    .identity-shell .form-control::placeholder {
        color: #607089;
    }

    .identity-input:focus,
    .identity-shell .form-control:focus {
        border-color: #38bdf8;
        background: #0b1220;
        color: #eef5ff;
        box-shadow: 0 0 0 .15rem rgba(56, 189, 248, .12);
    }

    .identity-primary-action,
    .identity-secondary-action,
    .identity-danger-action {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        min-height: 40px;
        border-radius: 8px;
        border: 1px solid transparent;
        padding: 0 16px;
        font-weight: 700;
        text-decoration: none;
        transition: transform .16s ease, box-shadow .16s ease, background .16s ease;
    }

    .identity-primary-action {
        color: #f4f7fb;
        background: #1d8cf8;
        box-shadow: 0 10px 24px rgba(29, 140, 248, .25);
    }

    .identity-primary-action:hover {
        color: #f4f7fb;
        background: #1673d1;
        transform: translateY(-1px);
    }

    .identity-secondary-action {
        color: #c8d4e6;
        background: #121d33;
        border-color: #2a3a5d;
    }

    .identity-secondary-action:hover {
        color: #f4f7fb;
        background: #18243b;
    }

    .identity-danger-action {
        color: #f4f7fb;
        background: #e11d48;
        border-color: #e11d48;
    }

    .identity-danger-action:hover {
        color: #f4f7fb;
        background: #be123c;
        border-color: #be123c;
    }

    .identity-table-card {
        overflow: hidden;
    }

    .identity-table {
        width: 100%;
        margin: 0;
        border-collapse: collapse;
        color: #dbe7f6;
    }

    .identity-table thead th {
        border: 0;
        background: #0b1220;
        color: #9aabc2;
        font-size: 12px;
        font-weight: 800;
        letter-spacing: .06em;
        text-transform: uppercase;
        padding: 14px 16px;
    }

    .identity-table tbody tr {
        background: #111a2e;
    }

    .identity-table tbody tr:hover {
        background: #142039;
    }

    .identity-table tbody td {
        border-top: 1px solid #24314f;
        padding: 16px;
        vertical-align: middle;
    }

    .identity-name-cell {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .identity-avatar {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 42px;
        height: 42px;
        border-radius: 8px;
        color: #38bdf8;
        background: #113653;
        font-weight: 800;
    }

    .identity-name-cell strong,
    .identity-name-cell small {
        display: block;
    }

    .identity-name-cell strong {
        color: #f4f7fb;
        font-weight: 800;
    }

    .identity-name-cell small {
        margin-top: 2px;
        color: #8ea0b8;
    }

    .identity-badge-list,
    .identity-action-group {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 8px;
    }

    .identity-badge {
        display: inline-flex;
        align-items: center;
        border-radius: 999px;
        color: #34d399;
        background: rgba(16, 185, 129, .12);
        padding: 6px 10px;
        font-size: 12px;
        font-weight: 700;
    }

    .identity-action-group.justify-content-end {
        justify-content: flex-end;
    }

    .identity-table .identity-secondary-action,
    .identity-table .identity-danger-action {
        min-height: 38px;
        padding: 0 12px;
        font-size: 13px;
    }

    .identity-empty {
        padding: 36px 24px;
        color: #8ea0b8;
        text-align: center;
    }

    .identity-empty i {
        display: block;
        margin-bottom: 12px;
        color: #38bdf8;
        font-size: 34px;
    }

    .identity-empty strong {
        color: #f4f7fb;
        font-size: 18px;
    }

    .identity-panel {
        padding: 22px;
    }

    .identity-form-grid,
    .identity-readonly-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 18px;
    }

    .identity-form label,
    .identity-readonly-label {
        display: block;
        margin-bottom: 8px;
        color: #c8d4e6;
        font-weight: 700;
    }

    .identity-readonly-value {
        min-height: 42px;
        border: 1px solid #2a3a5d;
        border-radius: 8px;
        background: #0f172a;
        padding: 11px 12px;
        color: #eef5ff;
        font-weight: 700;
        overflow-wrap: anywhere;
    }

    .identity-role-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 14px;
    }

    .identity-role-option {
        display: flex;
        align-items: center;
        gap: 10px;
        border: 1px solid #24314f;
        border-radius: 8px;
        background: #111a2e;
        padding: 14px;
        color: #dbe7f6;
        font-weight: 700;
    }

    .identity-role-option input {
        width: 18px;
        height: 18px;
        accent-color: #1d8cf8;
    }

    .identity-shell .text-danger,
    .identity-shell .field-validation-error,
    .identity-shell [data-valmsg-for] {
        color: #fb7185 !important;
        font-size: 13px;
    }

    @@media (max-width: 860px) {
        .identity-toolbar,
        .identity-search,
        .identity-action-group {
            align-items: stretch;
            flex-direction: column;
        }

        .identity-table-card {
            overflow-x: auto;
        }

        .identity-table {
            min-width: 760px;
        }

        .identity-primary-action,
        .identity-secondary-action,
        .identity-danger-action {
            width: 100%;
        }
    }
</style>
