@model RoleViewModel

<div class="identity-readonly-grid">
    <div>
        <div class="identity-readonly-label">Role Id</div>
        <div class="identity-readonly-value">@Model.Id</div>
    </div>

    <div>
        <div class="identity-readonly-label">Role Name</div>
        <div class="identity-readonly-value">@Model.Name</div>
    </div>
</div>
